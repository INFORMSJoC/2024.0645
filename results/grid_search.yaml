# results/config_overrides/grid_search.yaml
defaults:
  - base_config
  - _self_

training:
  optimizer:
    lr: 1e-5
    weight_decay: 0.01
  batch_size: 32
  max_steps: 10000

model:
  hidden_size: 768
  num_attention_heads: 12
  num_hidden_layers: 12
